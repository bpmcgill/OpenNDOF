<ui:FluentWindow x:Class="OpenNDOF.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:views="clr-namespace:OpenNDOF.App.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="OpenNDOF Bridge"
        MinWidth="900" MinHeight="600"
        Width="1200" Height="750"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title bar with Mica/Mica-Alt blending -->
        <ui:TitleBar Grid.Row="0"
                     Title="OpenNDOF Bridge"
                     ShowMaximize="True"
                     ShowMinimize="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <!-- Navigation shell -->
        <ui:NavigationView Grid.Row="1"
                           x:Name="RootNavigation"
                           PaneDisplayMode="Left"
                           IsPaneToggleVisible="True"
                           OpenPaneLength="200">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="Dashboard"
                                       TargetPageType="{x:Type views:DashboardView}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Home24"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

                <ui:NavigationViewItem Content="Configuration"
                                       TargetPageType="{x:Type views:ConfigurationView}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Settings24"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

                <ui:NavigationViewItem Content="Test Input"
                                       TargetPageType="{x:Type views:TestInputView}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Joystick20"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

                <ui:NavigationViewItem Content="LCD Display"
                                       TargetPageType="{x:Type views:LcdView}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="TextDescription24"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="About"
                                       TargetPageType="{x:Type views:AboutView}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Info24"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.FooterMenuItems>

        </ui:NavigationView>

        <!-- Snackbar presenter (overlaid on the content area) -->
        <ui:SnackbarPresenter x:Name="SnackbarPresenter"
                              Grid.Row="1"
                              VerticalAlignment="Bottom"
                              HorizontalAlignment="Stretch"
                              Margin="0,0,0,12"/>

    </Grid>
</ui:FluentWindow>
